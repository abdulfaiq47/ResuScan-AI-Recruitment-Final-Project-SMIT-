# ── ResuScan — dependencies ───────────────────────────────────────────────
streamlit>=1.36.0
pandas>=2.0.0

# LangChain core + providers
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.7          # used for OpenRouter + NVIDIA NIM (OpenAI-compatible)
langchain-google-genai>=1.0.5    # used for Google Gemini

# PDF reading
pypdf>=4.2.0
pdfplumber>=0.11.0               # fallback extractor for tricky PDFs

# Exports
python-docx>=1.1.0               # Word (.docx) candidate summary export

# Misc
requests>=2.31.0
python-dotenv>=1.0.0
